object Main extends App {
  // MainBackup.run()
  // Read the JSON file
  // Prompt the user to enter a value
  // Create a function to make HTTP requests

  // Creating an empty mutable HashMap



  // Example usage
  val list = LinkedList(1, 2, 3, 4, 5)
  println(
    list
  ) // Prints: ListNode(1,ListNode(2,ListNode(3,ListNode(4,ListNode(5,EmptyList))))))

}
